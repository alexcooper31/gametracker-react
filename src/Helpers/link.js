import styled from 'styled-components';
import { Link as Ref } from "react-router-dom";

export default styled(Ref)`
  text-decoration: none;
  color: inherit;

  &:active {
    color: inherit;
  }
`;